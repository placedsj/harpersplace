import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function FundPage() {
  return (
    <div className="container mx-auto py-8">
      <Card>
        <CardHeader>
          <CardTitle>Harper's Fund</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Financial management features are currently under maintenance.</p>
        </CardContent>
      </Card>
    </div>
  );
}
