# ============================================================
# Harper's Place — Environment Variables Template
# ============================================================
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# For Vercel deployment, add each variable in:
#   Vercel Dashboard → Project → Settings → Environment Variables
#
# Never commit your actual .env.local to version control.
# ============================================================


# ============================================================
# Firebase Configuration (REQUIRED)
# ============================================================
# All NEXT_PUBLIC_* variables are safe to expose to the browser.
#
# How to get these values:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Click ⚙ → Project Settings → Your Apps → Web App
# 4. Copy the values from the firebaseConfig object
# ============================================================

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=


# ============================================================
# Google Gemini AI (OPTIONAL — enables AI features)
# ============================================================
# Used server-side only. Powers expense categorization,
# schedule optimization, communication coach, and more.
#
# How to get your key:
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Sign in with your Google account
# 3. Click "Create API Key" and copy it here
# ============================================================

GEMINI_API_KEY=


# ============================================================
# Firebase Admin SDK (OPTIONAL — enables server-side admin)
# ============================================================
# Required only if you use server-side Firebase Admin features.
# Paste the full JSON content of your service account key file
# as a single-line JSON string (escape quotes as needed), or
# leave blank if not needed.
#
# How to get it:
# 1. Firebase Console → Project Settings → Service Accounts
# 2. Click "Generate new private key"
# 3. Paste the content of the downloaded JSON file below
# ============================================================

# FIREBASE_SERVICE_ACCOUNT_KEY=
